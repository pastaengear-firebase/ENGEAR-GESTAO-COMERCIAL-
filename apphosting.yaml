# apphosting.yaml
# Forçando um novo build para resolver o erro "Signed URL Expired".
runtime: nodejs20

runConfig:
  cpu: 1
  memoryMiB: 512
env:
  - variable: NEXT_PHASE
    value: "phase-production-build"
  - variable: NODE_ENV
    value: "production"
